# Versions tested for this release.
# Figure + analysis reproduction (with the included data/raw/*.npz and results/*.npz) is CPU-only.
# Regenerating the RCWA datasets additionally requires a CUDA GPU and torcwa.
torch==2.10.0        # RCWA generation used torch 2.5.1 + CUDA; figure/analysis reproduction on 2.10.0 (CPU). Any torch>=2.0 suffices for reproduction.
numpy==1.26.4
scipy==1.11.4
matplotlib==3.10.0
torcwa==0.1.4.2      # only required to regenerate RCWA data (needs CUDA)
tqdm>=4.62.0
